# Terraform cache and providers
.terraform/
.terraform.lock.hcl
 
# State files (sensitive - never commit)
*.tfstate
*.tfstate.backup
 
# Plan files
tfplan
*.tfplan
 
# Credentials (never commit)
*.tfvars
secrets.tf
 
